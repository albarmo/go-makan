diff --git a/node_modules/@solidjs/vite-plugin-nitro-2/dist/index.js b/node_modules/@solidjs/vite-plugin-nitro-2/dist/index.js
index 9d82650..528e6bb 100644
--- a/node_modules/@solidjs/vite-plugin-nitro-2/dist/index.js
+++ b/node_modules/@solidjs/vite-plugin-nitro-2/dist/index.js
@@ -85,9 +85,20 @@ export function nitroV2Plugin(nitroConfig) {
                                 },
                                 virtual: {
                                     ...nitroConfig?.virtual,
-                                    [virtualEntry]: `import { fromWebHandler } from 'h3'
-                                  import handler from '${ssrEntryFile}'
-                                  export default fromWebHandler(handler.fetch)`
+                                    [virtualEntry]: `import handler from '${ssrEntryFile}'
+                                  export default function(event) {
+                                    const nodeReq = event.node?.req || event.req;
+                                    const h = typeof nodeReq?.headers === 'object' ? nodeReq.headers : {};
+                                    const host = h.host || h[':authority'] || 'localhost';
+                                    const proto = h['x-forwarded-proto'] || (nodeReq?.socket?.encrypted ? 'https' : 'http');
+                                    const url = proto + '://' + host + (nodeReq?.url || '/');
+                                    const headers = {};
+                                    for (const [k, v] of Object.entries(h)) {
+                                      if (v != null) headers[k] = Array.isArray(v) ? v.join(', ') : String(v);
+                                    }
+                                    const request = new Request(url, { method: nodeReq?.method || 'GET', headers: new Headers(headers) });
+                                    return handler.fetch(request, event.context || event);
+                                  }`
                                 }
                             };
                             const nitro = await createNitro(resolvedNitroConfig);
